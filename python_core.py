""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
#
# 🔹 Что такое decorator?
# 📌 Простыми словами:
#
# Декоратор — это функция, которая изменяет поведение другой функции.
#
# То есть:
#
# берём функцию
# оборачиваем её
# добавляем что-то ДО или ПОСЛЕ выполнения
# 🔥 1️⃣ База — функция внутри функции
# def wrapper():
#     print("Before")
#
#     def inner():
#         print("Hello")
#
#     # inner()
#     print("After")
#
# wrapper()
#
# Вывод:
#
# Before
# Hello
# After
# 🔥 2️⃣ Передаём функцию как аргумент
# def greet():
#     print("Hello!")
#
# def decorator(func):
#     print("Before function")
#     func()
#     print("After function")
#
# decorator(func=greet)
# 🔥 3️⃣ Настоящий декоратор
#
# Вот правильная структура:
#
# def decorator(func):
#     def wrapper():
#         print("Before")
#         func()
#         print("After")
#     return wrapper
#
# Использование:
#
# def say_hello():
#     print("Hello!")
# #
# say_hi = decorator(say_hello)
#
# say_hi()
# 🔥 4️⃣ Синтаксис @
#
# В Python есть короткая запись:
#
# def decorator(func):
#     def wrapper():
#         print("Before")
#         func()
#         print("After")
#     return wrapper
# #
# #
# @decorator
# def say_hello():
#     print("Hello!")
#
# say_hello()
#
# Это то же самое что:
#
# say_hello = decorator(say_hello)
# 🔥 5️⃣ Декоратор с аргументами
#
# Это очень важно для интервью.
#
# def plus_one_every_time(func):
#     def wrapper(*args, **kwargs):
#         print("Before")
#         result = func(*args, **kwargs)
#         new_result = result + 1
#         print("After")
#         return new_result
#     return wrapper
#
#
# @plus_one_every_time
# def add(a, b):
#     return a + b
#
#
# print(add(5, 5))
#
# Почему *args, **kwargs?
#
# Чтобы декоратор работал с любой функцией.
#
# 🔥 6️⃣ functools.wraps (очень любят спрашивать)
#
# Без wraps имя функции меняется:
#
# print(add.__name__)
#
# Будет:
#
# wrapper

""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
#
# 🔹 Что такое generator?
#
# Generator — это функция, которая возвращает значения по одному, а не всё сразу.
#
# Он использует ключевое слово:
#
# yield
#
# Главная идея:
#
# return → возвращает значение и завершает функцию
#
# yield → возвращает значение и "замораживает" функцию
#
# 🔥 Простой пример
# def my_generator():
#     yield 1
#     yield 2
#     yield 3
#
#     # return 1
#     # return 2
#     # return 3
#
#
# gen = my_generator()
#
# print(gen)
# print(type(gen))
#
# for value in gen:
#     print(value)
#
# Вывод:
#
# 1
# 2
# 3
# 🔥 Как это работает?
#
# Когда вызываем:

# gen = my_generator()
#
# Функция не выполняется сразу.
# Она создаёт generator-объект.
#
# Когда начинаем перебирать (for или next()),
# она выполняется до первого yield.
#
# 🔥 Пример с next()
# def count():
#     yield 10
#     yield 20
#     yield 30
#
#
# g = count()
#
# print(next(g))  # 10
# print(next(g))  # 20
# print(next(g))  # 30
#
# Если вызвать ещё раз → будет ошибка StopIteration.
#
# 🔥 Зачем они нужны?
#
# Главная причина — экономия памяти.
#
# Сравни:
#
# ❌ Обычный список
numbers = [i for i in range(1_000_000)]
# print(numbers)
# Создаст миллион чисел в памяти.
#
# ✅ Generator
numbers = (i for i in range(1_000_000))

# for n in numbers:
#     print(n)
#
# Он создаёт числа по одному, когда нужно.
#
# 🔥 Где используются?
#
# работа с большими файлами
#
# чтение строк из файла
#
# обработка больших данных
#
# streaming
#
# backend (когда нужно не грузить всё в память)
#
# 🎯 Частый вопрос на интервью
#
# ❓ В чём разница между list и generator?
#
# Ответ:
#
# list	                                generator
# хранит всё в памяти	            генерирует по одному
# быстрее при повторном доступе	        экономит память
# []	                                    ()
# 💪 Если кратко
#
# Generator = ленивый список
# Работает через yield
# Экономит память

""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
#
# 🔹 Что такое Context Manager?
#
# Это механизм, который:
#
# 👉 автоматически управляет ресурсами
# 👉 выполняет код ДО и ПОСЛЕ блока
#
# Используется через:
#
# with
# 🔥 Самый простой пример
# with open("test.txt", "w") as riter:
#     riter.write("adsfa")
#
# Что происходит?
#
# Файл открывается
#
# Выполняется код внутри блока
#
# Файл автоматически закрывается
#
# Без with пришлось бы писать:

# f = open("test.txt", "w")
# f.write("Hello")
# f.close()
# 🔥 Зачем нужен?
#
# Чтобы:
#
# закрывать файлы
#
# закрывать соединения с БД
#
# освобождать ресурсы
#
# работать с lock'ами
#
# управлять транзакциями
#
# 🔥 Как работает внутри?
#
# Context manager имеет 2 метода:
#
# __enter__()
# __exit__()
# 🔥 Свой context manager (краткий пример)
# class MyContext:
#     def __enter__(self):
#         print("Start")
#         return self
#
#     def __exit__(self, exc_type, exc_val, exc_tb):
#         print("End")
#
#
# with MyContext():
#     print("Inside block")
#
# Вывод:
#
# Start
# Inside block
# End
# 🔥 Кратко для интервью
#
# Context Manager — это объект, который:
#
# реализует __enter__ и __exit__
#
# используется через with
#
# управляет ресурсами автоматически
#
# 🔥 Частый вопрос
#
# ❓ Что будет если внутри блока ошибка?
#
# Ответ:
# __exit__ всё равно выполнится.

""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
#
# 🔹 Что это такое?
#
# Это специальные методы в Python вида:
#
# __something__
#
# Примеры:
#
# __init__
#
# __str__
#
# __len__
#
# __add__
#
# __repr__
#
# Они позволяют объектам вести себя как встроенные типы.
#
# 🔥 1️⃣ __init__ — конструктор
#
# Вызывается при создании объекта.

# class User:
#     def __init__(self, name):
#         self.name = name
#
# u = User("Nuri")
# print(u.name)
# 🔥 2️⃣ __str__ — красивый вывод для print()
#
# Без него:

# class User:
#     def __init__(self, name):
#         self.name = name
#
# u = User("Nuri")
# print(u)

# Вывод будет что-то вроде:
#
# <__main__.User object at 0x...>
#
# Добавим __str__:

# class User:
#     def __init__(self, name):
#         self.name = name
#
#     def __str__(self):
#         return f"User: {self.name}"
#
# u = User("Nuri")
# print(u)

# Теперь:

# User: Nuri
# 🔥 3️⃣ __repr__ — представление для разработчиков
#
# Используется в интерактивной консоли.
#
# class User:
#     def __init__(self, name):
#         self.name = name
#
#     def __repr__(self):
#         return f"User(name='{self.name}')"
#
# u = User("Nuri")
# print(repr(u))
# # 🔥 4️⃣ __len__ — позволяет использовать len()
# class MyList:
#     def __init__(self, items):
#         self.items = items
#
#     def __len__(self):
#         return len(self.items)
#
# obj = MyList([1, 2, 3, 4])
# print(len(obj))
# # 🔥 5️⃣ __add__ — перегрузка оператора +
# class Number:
#     def __init__(self, value):
#         self.value = value
#
#     def __add__(self, other):
#         return Number(self.value + other.value)
#
# n1 = Number(10)
# n2 = Number(20)
#
# result = n1 + n2
# print(result.value)

# Теперь + работает для наших объектов.
#
# 🔥 6️⃣ __eq__ — сравнение ==
# class User:
#     def __init__(self, name):
#         self.name = name
#
#     def __eq__(self, other):
#         return self.name == other.name
#
# u1 = User("Nuri")
# u2 = User("Nuri")
#
# print(u1 == u2)
# # 🔥 7️⃣ __call__ — объект можно вызвать как функцию
# class Greeter:
#     def __call__(self):
#         print("Hello!")
#
# g = Greeter()
# g()
# 🎯 Что важно знать для интервью
#
# Что такое dunder методы?
#
# Зачем нужен __str__?
#
# Разница между __str__ и __repr__?
#
# Как перегрузить оператор +?
#
# Можно ли сделать объект вызываемым?
#
# 🔥 Кратко
#
# Dunder методы позволяют:
#
# управлять созданием объекта
#
# изменять поведение операторов
#
# управлять выводом
#
# делать объекты "умными"

""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
#
# 🔹 Что такое Exception?
#
# Exception (исключение) — это ошибка во время выполнения программы.
#
# Пример ошибки:

# print(10 / 0)
#
# Получим:
#
# ZeroDivisionError
# 🔥 1️⃣ try / except
#
# Используется для обработки ошибок.

# try:
#     x = 10 / 0
# except:
#     print("Нельзя делить на ноль")

# Теперь программа не упадёт.
#
# 🔥 2️⃣ Обработка разных ошибок
# try:
#     num = float("12323423.7")
#     print(num)
# except ValueError:
#     print("Это не число")
# 🔥 3️⃣ Несколько except
# try:
#     x = int(input("Введите число: "))
#     print(10 / x)
# except ValueError:
#     print("Ошибка: не число")
# except ZeroDivisionError:
#     print("Ошибка: деление на ноль")
# 🔥 4️⃣ except Exception
#
# Перехватывает любую ошибку.

# try:
#     x = 10 / 0
# except Exception as e:
#     print("Ошибка:", e)
#
# x = 10
# y = 1231
# print(f"{y}")
# print(f"Hello {x}")
# ⚠ На интервью важно сказать:
# Лучше ловить конкретные ошибки, а не просто Exception.
#
# 🔥 5️⃣ else
#
# Выполняется если ошибки НЕ было.

# try:
#     x = 10 / 2
# except ZeroDivisionError:
#     print("Ошибка")
# else:
#     print("Все прошло успешно:", x)
# 🔥 6️⃣ finally

# Выполняется ВСЕГДА.

# try:
#     x = 10 / 0
# except ZeroDivisionError:
#     print("Ошибка")
# finally:
#     print("Этот код выполнится всегда")
# 🔥 7️⃣ raise — вызвать свою ошибку
# age = -5
#
# if age < 0:
#     raise ValueError("Возраст не может быть отрицательным")
# # 🔥 8️⃣ Своя Exception
# class MyError(Exception):
#     pass
#
#
# def check_number(num):
#     if num < 0:
#         raise MyError("Число не может быть отрицательным")
#
#
# try:
#     check_number(-1)
# except MyError:
#     print("Поймали ошибку")
# 🔥 Иерархия ошибок
#
# Все ошибки наследуются от:
#
# BaseException
#    └── Exception
#
# Примеры:
#
# ValueError
#
# TypeError
#
# ZeroDivisionError
#
# IndexError
#
# KeyError
#
# 🎯 Что спрашивают на интервью
#
# Разница между Error и Exception?
#
# Что делает finally?
#
# В чём разница except Exception и except конкретная ошибка?
#
# Как создать свою ошибку?
#
# Что делает raise?
#
# 🔥 Кратко
#
# try → пробуем
# except → ловим ошибку
# else → если всё хорошо
# finally → выполняется всегда
# raise → создаём ошибку

""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""

# 🔹 1️⃣ List Comprehension

# Обычный способ:

# numbers = []
#
# for num in range(5):
#     numbers.append(num)
#
# print(numbers)
#
# Через comprehension:
#
# numbers = [i for i in range(5)]
# print(numbers)
#
# 📌 Формула:
#
# [что_добавить for элемент in итерируемый_объект]
# 🔥 С условием
# even_numbers = [i for i in range(10) if i % 2 == 0]
# print(even_numbers)
#
# 👉 Добавляем только чётные числа.
#
# 🔥 С преобразованием
# squares = [i * i for i in range(5)]
# print(squares)
# 🔹 2️⃣ Set Comprehension
# numbers = {i for i in range(5)}
# print(numbers)
#
# С условием:
#
# even_set = {i for i in range(10) if i % 2 == 0}
# print(even_set)
# 🔹 3️⃣ Dict Comprehension
# squares_dict = {i: i * i for i in range(5)}
# print(squares_dict)
#
# 👉 ключ : значение
#
# 🔹 4️⃣ Generator Comprehension
#
# Похоже на list comprehension, но в круглых скобках:
#
# gen = (i for i in range(5))
#
# print(gen)
# print(list(gen))
#
# ⚠ Это generator — не хранит всё в памяти.
#
# 🔥 Вложенные comprehensions
# pairs = [(x, y) for x in range(2) for y in range(2)]
# print(pairs)
# 🎯 Частые вопросы на интервью
#
# Чем list comprehension лучше обычного цикла?
#
# короче
#
# быстрее
#
# более читаемый (если не усложнять)
#
# В чём разница:
#
# [i for i in ...]
#
# и
#
# (i for i in ...)
#
# → первое список
# → второе generator
#
# Можно ли писать слишком сложные comprehensions?
# → Можно, но не рекомендуется (ухудшается читаемость)
#
# 🔥 Кратко
# Тип	Скобки
# list	[]
# set	{}
# dict	{k: v}
# generator	()

""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""



""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""